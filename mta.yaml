_schema-version: "3.2"
ID: myfioriapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: myfioriapp-destination-content
  type: com.sap.application.content
  requires:
  - name: myfioriapp-destination-service
    parameters:
      content-target: true
  - name: myfioriapp-repo-host
    parameters:
      service-key:
        name: myfioriapp-repo-host-key
  - name: myfioriapp-uaa
    parameters:
      service-key:
        name: myfioriapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: myfioriapp_html_repo_host
          ServiceInstanceName: myfioriapp-html5-service
          ServiceKeyName: myfioriapp-repo-host-key
          sap.cloud.service: myfioriapp
        - Authentication: OAuth2UserTokenExchange
          Name: myfioriapp_uaa
          ServiceInstanceName: myfioriapp-xsuaa-service
          ServiceKeyName: myfioriapp-uaa-key
          sap.cloud.service: myfioriapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: myfioriapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: myfioriapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - myfioriapp.zip
      name: myfioriapp
      target-path: resources/
- name: myfioriapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: myfioriapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: myfioriapp-destination-service
    service-plan: lite
- name: myfioriapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: myfioriapp-xsuaa-service
    service-plan: application
- name: myfioriapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: myfioriapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
